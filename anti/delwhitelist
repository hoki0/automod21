const Command = require('../Command.js');
const { MessageEmbed } = require('discord.js');
const fetch = require('node-fetch');

module.exports = class DelWhitelistCommand extends Command {
  constructor(client) {
    super(client, {
      name: 'delwhitelist',
      usage: 'delwhitelist',
      description: 'Shows the antinuke delwhitelists ',
      examples: [],
      type: client.types.ANTI
    });
  }
  async run(message) {
    }
  };
